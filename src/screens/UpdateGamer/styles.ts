import styled from "styled-components/native";

export const RegisterGamerContainer = styled.View`
background-color: ${ ({ theme }) => theme.COLORS.BACKGROUND };
flex: 1;
row-gap: ${ ({ theme }) => theme.SPACING.MEDIUM };
`